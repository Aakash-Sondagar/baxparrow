# Task 4 Review Package
## Grep evidence

apps\web\src\routes\admin\Categories.tsx:1:import { tile } from "../../styles/tokens";
apps\web\src\routes\admin\Products.tsx:2:import { tile } from "../../styles/tokens";




apps\web\src\routes\admin\Categories.tsx:12:          <div className="h-24" style={{ background: tile(i) }} />
apps\web\src\routes\admin\Dashboard.tsx:55:                <Tooltip formatter={(value: any) => [inr(Number(value)), 
"Revenue"]} labelStyle={{ color: "#1C1917", fontWeight: 700 }} />
apps\web\src\routes\admin\Dashboard.tsx:77:                  <div className="h-full bg-cognac transition-[width] 
duration-[400ms] ease-in-out" style={{ width: `${pct}%` }} />
apps\web\src\routes\admin\Products.tsx:23:              <td className="px-[22px] py-3"><div className="flex 
items-center gap-3"><span className="h-10 w-10 shrink-0 rounded-[9px]" style={{ background: tile(i) }} /><span 
className="font-semibold">{p.name}</span></div></td>




## File list changed vs snapshot


 ..."apps\\web\\src\\routes\\admin\\AdminLogin.tsx" | 27 +++++++++++-----------
 1 file changed, 13 insertions(+), 14 deletions(-)

 ..."apps\\web\\src\\routes\\admin\\BulkUpload.tsx" | 43 +++++++++++-----------
 1 file changed, 21 insertions(+), 22 deletions(-)

 ..."apps\\web\\src\\routes\\admin\\Categories.tsx" | 28 +++++++++++-----------
 1 file changed, 14 insertions(+), 14 deletions(-)

 ... "apps\\web\\src\\routes\\admin\\Dashboard.tsx" | 86 +++++++++++-----------
 1 file changed, 45 insertions(+), 41 deletions(-)

 ... => "apps\\web\\src\\routes\\admin\\Orders.tsx" | 40 ++++++++++++----------
 1 file changed, 22 insertions(+), 18 deletions(-)

 ...apps\\web\\src\\routes\\admin\\ProductForm.tsx" | 77 +++++++++++-----------
 1 file changed, 38 insertions(+), 39 deletions(-)

 ...> "apps\\web\\src\\routes\\admin\\Products.tsx" | 36 +++++++++++-----------
 1 file changed, 18 insertions(+), 18 deletions(-)



## Dashboard.tsx full diff (representative)

diff --git "a/.superpowers\\sdd\\snapshots\\task-4-before\\Dashboard.tsx" "b/apps\\web\\src\\routes\\admin\\Dashboard.tsx"
index f9fd160..72de17e 100644
--- "a/.superpowers\\sdd\\snapshots\\task-4-before\\Dashboard.tsx"
+++ "b/apps\\web\\src\\routes\\admin\\Dashboard.tsx"
@@ -1,13 +1,17 @@
 import { BarChart, Bar, XAxis, Tooltip, ResponsiveContainer, Cell } from "recharts";
-import { t } from "../../styles/tokens";
 import { inr } from "../../lib/format";
 import { useMetrics, useAdminOrders } from "../../features/orders/adminHooks";
 
 const badge = (s: string) => {
-  const m: Record<string,[string,string]> = { delivered:[t.greenBg,t.green], shipped:["#E9EEF6","#3B6DB0"], processing:["#FBF1E5",t.amber], pending:["#F3EFEA",t.muted] };
-  const [bg, fg] = m[s] ?? m.pending; return { background: bg, color: fg, fontSize: 12, fontWeight: 700, padding: "4px 11px", borderRadius: 20 };
+  const m: Record<string, string> = {
+    delivered: "bg-green-bg text-green",
+    shipped: "bg-[#E9EEF6] text-[#3B6DB0]",
+    processing: "bg-[#FBF1E5] text-amber",
+    pending: "bg-[#F3EFEA] text-muted",
+  };
+  return `text-xs font-bold px-[11px] py-1 rounded-full ${m[s] ?? m.pending}`;
 };
 const cap = (s: string) => s ? s[0].toUpperCase() + s.slice(1) : "";
 
 export default function Dashboard() {
   const { data: m } = useMetrics();
@@ -19,93 +23,93 @@ export default function Dashboard() {
   ];
 
   const topCategories = m?.topCategories && m.topCategories.length > 0 ? m.topCategories : [];
 
   const stats: [string, string, string, string][] = [
-    ["Revenue (30d)", m ? inr(m.revenue) : "Gé¦0", m?.revenue ? "Gû¦ Live Total" : "No sales yet", t.green],
-    ["Orders", m ? String(m.orders) : "0", m?.orders ? "Gû¦ Total orders" : "No orders yet", t.green],
-    ["Products live", m ? String(m.products) : "0", `${m?.lowStock ?? 0} low stock`, t.amber],
-    ["Avg order value", m ? inr(m.aov) : "Gé¦0", "Average per order", t.cognac],
+    ["Revenue (30d)", m ? inr(m.revenue) : "Gé¦0", m?.revenue ? "Gû¦ Live Total" : "No sales yet", "text-green"],
+    ["Orders", m ? String(m.orders) : "0", m?.orders ? "Gû¦ Total orders" : "No orders yet", "text-green"],
+    ["Products live", m ? String(m.products) : "0", `${m?.lowStock ?? 0} low stock`, "text-amber"],
+    ["Avg order value", m ? inr(m.aov) : "Gé¦0", "Average per order", "text-cognac"],
   ];
 
   const recent = orders.slice(0, 5);
 
   return (
     <>
-      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))", gap: 16, marginBottom: 22 }}>
+      <div className="mb-[22px] grid grid-cols-[repeat(auto-fit,minmax(220px,1fr))] gap-4">
         {stats.map(([l, v, d, c]) => (
-          <div key={l} style={{ background: "#fff", border: `1px solid ${t.border}`, borderRadius: 14, padding: 20 }}>
-            <div style={{ fontSize: 13, color: t.muted, marginBottom: 10 }}>{l}</div>
-            <div style={{ fontFamily: t.displayFont, fontWeight: 800, fontSize: 26 }}>{v}</div>
-            <div style={{ fontSize: 12.5, color: c, marginTop: 6, fontWeight: 600 }}>{d}</div>
+          <div key={l} className="rounded-[14px] border border-border bg-card p-5">
+            <div className="mb-2.5 text-[13px] text-muted">{l}</div>
+            <div className="font-display text-[26px] font-extrabold">{v}</div>
+            <div className={`mt-1.5 text-[12.5px] font-semibold ${c}`}>{d}</div>
           </div>
         ))}
       </div>
 
-      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(300px, 1fr))", gap: 16, marginBottom: 22 }}>
-        <div style={{ background: "#fff", border: `1px solid ${t.border}`, borderRadius: 14, padding: 22 }}>
-          <div style={{ fontFamily: t.displayFont, fontWeight: 700, fontSize: 16, marginBottom: 20 }}>Revenue -+ last 7 days</div>
-          <div style={{ height: 210 }}>
+      <div className="mb-[22px] grid grid-cols-[repeat(auto-fit,minmax(300px,1fr))] gap-4">
+        <div className="rounded-[14px] border border-border bg-card p-[22px]">
+          <div className="mb-5 font-display text-base font-bold">Revenue -+ last 7 days</div>
+          <div className="h-[210px]">
             <ResponsiveContainer width="100%" height="100%">
               <BarChart data={chartData}>
-                <XAxis dataKey="day" axisLine={false} tickLine={false} tick={{ fontFamily: "monospace", fontSize: 11, fill: t.muted2 }} />
-                <Tooltip formatter={(value: any) => [inr(Number(value)), "Revenue"]} labelStyle={{ color: t.ink, fontWeight: 700 }} />
+                <XAxis dataKey="day" axisLine={false} tickLine={false} tick={{ fontFamily: "monospace", fontSize: 11, fill: "#A8A29E" }} />
+                <Tooltip formatter={(value: any) => [inr(Number(value)), "Revenue"]} labelStyle={{ color: "#1C1917", fontWeight: 700 }} />
                 <Bar dataKey="v" radius={[7, 7, 0, 0]}>
                   {chartData.map((_, i) => (
-                    <Cell key={i} fill={i === chartData.length - 1 ? t.cognac : "#E2C6A9"} />
+                    <Cell key={i} fill={i === chartData.length - 1 ? "#A94D28" : "#E2C6A9"} />
                   ))}
                 </Bar>
               </BarChart>
             </ResponsiveContainer>
           </div>
         </div>
 
-        <div style={{ background: "#fff", border: `1px solid ${t.border}`, borderRadius: 14, padding: 22 }}>
-          <div style={{ fontFamily: t.displayFont, fontWeight: 700, fontSize: 16, marginBottom: 18 }}>Top categories</div>
-          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
-            {topCategories.length === 0 && <div style={{ fontSize: 13, color: t.muted }}>No products categorized yet.</div>}
+        <div className="rounded-[14px] border border-border bg-card p-[22px]">
+          <div className="mb-[18px] font-display text-base font-bold">Top categories</div>
+          <div className="flex flex-col gap-4">
+            {topCategories.length === 0 && <div className="text-[13px] text-muted">No products categorized yet.</div>}
             {topCategories.map(({ name, pct }) => (
               <div key={name}>
-                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13.5, marginBottom: 6 }}>
-                  <span style={{ color: t.text2, fontWeight: 600 }}>{name}</span>
-                  <span style={{ fontFamily: t.monoFont, color: t.muted }}>{pct}%</span>
+                <div className="mb-1.5 flex justify-between text-[13.5px]">
+                  <span className="font-semibold text-text2">{name}</span>
+                  <span className="font-mono text-muted">{pct}%</span>
                 </div>
-                <div style={{ height: 8, borderRadius: 5, background: "#F0EBE3", overflow: "hidden" }}>
-                  <div style={{ height: "100%", background: t.cognac, width: `${pct}%`, transition: "width 0.4s ease" }} />
+                <div className="h-2 overflow-hidden rounded-[5px] bg-[#F0EBE3]">
+                  <div className="h-full bg-cognac transition-[width] duration-[400ms] ease-in-out" style={{ width: `${pct}%` }} />
                 </div>
               </div>
             ))}
           </div>
         </div>
       </div>
 
-      <div style={{ background: "#fff", border: `1px solid ${t.border}`, borderRadius: 14, overflow: "hidden" }}>
-        <div style={{ padding: "18px 22px", borderBottom: "1px solid #F0EBE3", fontFamily: t.displayFont, fontWeight: 700, fontSize: 16 }}>Recent orders</div>
-        <div style={{ overflowX: "auto" }}>
-          <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 13.5, minWidth: 600 }}>
+      <div className="overflow-hidden rounded-[14px] border border-border bg-card">
+        <div className="border-b border-b-[#F0EBE3] px-[22px] py-[18px] font-display text-base font-bold">Recent orders</div>
+        <div className="overflow-x-auto">
+          <table className="w-full min-w-[600px] border-collapse text-[13.5px]">
             <thead>
-              <tr style={{ textAlign: "left", color: t.muted2, fontFamily: t.monoFont, fontSize: 11 }}>
+              <tr className="text-left font-mono text-[11px] text-muted2">
                 {["ORDER", "CUSTOMER", "TOTAL", "STATUS", "DATE"].map(h => (
-                  <th key={h} style={{ padding: "12px 22px", fontWeight: 400 }}>{h}</th>
+                  <th key={h} className="px-[22px] py-3 font-normal">{h}</th>
                 ))}
               </tr>
             </thead>
             <tbody>
               {recent.length === 0 && (
                 <tr>
-                  <td colSpan={5} style={{ padding: 20, textAlign: "center", color: t.muted }}>
+                  <td colSpan={5} className="p-5 text-center text-muted">
                     No orders yet GÇö they'll appear here after checkout.
                   </td>
                 </tr>
               )}
               {recent.map((o: any) => (
-                <tr key={o.orderNo} style={{ borderTop: "1px solid #F0EBE3" }}>
-                  <td style={{ padding: "13px 22px", fontFamily: t.monoFont, color: t.cognac, fontWeight: 700 }}>{o.orderNo}</td>
-                  <td style={{ padding: 13, fontWeight: 600 }}>{o.address?.firstName ?? "Guest"}</td>
-                  <td style={{ padding: 13, fontFamily: t.monoFont }}>{inr(o.amounts?.total ?? 0)}</td>
-                  <td style={{ padding: 13 }}><span style={badge(o.status)}>{cap(o.status)}</span></td>
-                  <td style={{ padding: "13px 22px", color: t.muted }}>{new Date(o.createdAt).toLocaleDateString()}</td>
+                <tr key={o.orderNo} className="border-t border-t-[#F0EBE3]">
+                  <td className="px-[22px] py-[13px] font-mono font-bold text-cognac">{o.orderNo}</td>
+                  <td className="p-[13px] font-semibold">{o.address?.firstName ?? "Guest"}</td>
+                  <td className="p-[13px] font-mono">{inr(o.amounts?.total ?? 0)}</td>
+                  <td className="p-[13px]"><span className={badge(o.status)}>{cap(o.status)}</span></td>
+                  <td className="px-[22px] py-[13px] text-muted">{new Date(o.createdAt).toLocaleDateString()}</td>
                 </tr>
               ))}
             </tbody>
           </table>
         </div>

